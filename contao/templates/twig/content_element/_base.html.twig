{% extends "@Contao/content_element/_base.html.twig" %}

{# Attach the grid classes to the class attribute of the content element wrapper tag. #}
{% set element_css_classes = get_dma_grid_classes() %}
